{
  "name": "@eyedea-sockets/magiclink",
  "version": "0.0.1",
  "description": "Syncano Socket for generating magic links for users",
  "repository": "https://github.com/eyedea-io/syncano-socket-magiclink.git",
  "public": true,
  "author": "Eyedea <hello@eyedea.io> https://eyedea.io",
  "dependencies": {
    "@syncano/core": "latest",
    "mailgun-js": "^0.22.0",
    "moment": "^2.17.1"
  },
  "scripts": {
    "build": "npm install && npm run build:src && npm run build:env",
    "build:src": "sh ./bin/compile",
    "build:env": "sh ./bin/compile-env"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "8"
          }
        }
      ]
    ]
  }
}
